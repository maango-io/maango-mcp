# Version control
.git
.gitignore
.github

# Local Python state
.venv
venv
__pycache__
*.py[cod]
*.egg-info
.eggs
build
dist

# Tests + dev tooling
tests
pytest.ini
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov

# IDE / editor / OS
.vscode
.idea
.DS_Store
*.swp

# Local secrets & env
.env
.env.*
!.env.example

# Project-local Claude state
.claude

# Deploy artifacts not needed at runtime.
# (LICENSE + README.md are kept — pyproject.toml references them, so the
# builder stage needs them to install the project. They don't end up in
# the final runtime image because only /opt/venv is copied across.)
deploy
